!!! MAKE SURE TO RUN FROM GEMMA ENVIRONMENT !!!

To find the data generation file, in terminal (at the location where git clone was performed):

cd ejecta/physics/jet

Then run: python3 data_gen.py

The output will be a .npz file. Default number of LC generation is 1 million.
